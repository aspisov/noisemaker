early_stopping:
  _target_: lightning.pytorch.callbacks.EarlyStopping
  monitor: val/loss
  min_delta: 0.0
  patience: 3
  verbose: false
  mode: "min"
  strict: true
  check_finite: true
  stopping_threshold:
  divergence_threshold:
  check_on_train_epoch_end:
